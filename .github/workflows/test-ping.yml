name: test-ping

on:
  workflow_dispatch:

jobs:
  build:

    runs-on: ubuntu-24.04

    defaults:
      run:
        shell: bash -el {0}

    steps:
    - name: Print resolv.conf
      continue-on-error: true
      run: |
        cat /etc/resolv.conf

    - name: Ping
      continue-on-error: true
      run: |
        curl --head https://e2sateditor.com
